<%- include('../../partials/header') %>

<div class="layout-wrapper layout-content-navbar">
  <div class="layout-container">
    <%- include('../../partials/sidebar') %>

    <div class="layout-page">
      <%- include('../../partials/navbar') %>

      <div class="content-wrapper">
        <div class="container-xxl flex-grow-1 container-p-y">
          <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4">
            <div>
              <h4 class="fw-bold mb-1">
                <span class="text-muted fw-light">Subcategories /</span> Archive
              </h4>
              <p class="mb-0 text-muted">Restore archived subcategories or remove records permanently.</p>
            </div>
            <a href="/subcategory/view" class="btn btn-primary">
              <i class="ri ri-arrow-left-line me-1"></i>
              Back to Active
            </a>
          </div>

          <div class="card">
            <div class="card-header border-bottom d-flex align-items-center gap-3">
              <div class="avatar">
                <div class="avatar-initial bg-label-warning rounded">
                  <i class="ri ri-archive-line icon-24px"></i>
                </div>
              </div>
              <div>
                <h5 class="mb-0">Archived Subcategories</h5>
                <small class="text-muted">Showing <%= subcategories ? subcategories.length : 0 %> archived records</small>
              </div>
            </div>

            <div class="card-body p-0">
              <div class="table-responsive text-nowrap">
                <table class="table table-hover align-middle mb-0">
                  <thead>
                    <tr>
                      <th>Subcategory</th>
                      <th>Parent Category</th>
                      <th>Status</th>
                      <th class="text-end">Actions</th>
                    </tr>
                  </thead>

                  <tbody class="table-border-bottom-0">
                    <% if (subcategories && subcategories.length > 0) { %>
                      <% subcategories.forEach(item => { %>
                        <tr>
                          <td>
                            <div class="d-flex align-items-center">
                              <div class="avatar avatar-sm me-3">
                                <div class="avatar-initial bg-label-secondary rounded">
                                  <i class="ri ri-node-tree"></i>
                                </div>
                              </div>
                              <span class="fw-medium text-heading"><%= item.subcategory %></span>
                            </div>
                          </td>
                          <td><%= item.category ? item.category.category : '-' %></td>
                          <td>
                            <span class="badge bg-label-warning">Archived</span>
                          </td>
                          <td class="text-end">
                            <div class="d-flex justify-content-end gap-2">
                              <a
                                href="/subcategory/restore/<%= item._id %>"
                                class="btn btn-sm btn-outline-success"
                                onclick="confirmAction(event, this.href, 'restore', 'subcategory')">
                                <i class="ri ri-arrow-go-back-line me-1"></i>
                                Restore
                              </a>
                              <a
                                href="/subcategory/permanent-delete/<%= item._id %>"
                                class="btn btn-sm btn-outline-danger"
                                onclick="confirmAction(event, this.href, 'delete', 'subcategory')">
                                <i class="ri ri-delete-bin-7-line me-1"></i>
                                Delete
                              </a>
                            </div>
                          </td>
                        </tr>
                      <% }) %>
                    <% } else { %>
                      <tr>
                        <td colspan="4" class="text-center py-5">
                          <div class="avatar avatar-xl mx-auto mb-3">
                            <div class="avatar-initial bg-label-secondary rounded-circle">
                              <i class="ri ri-archive-2-line icon-28px"></i>
                            </div>
                          </div>
                          <h6 class="mb-1">Archive is Empty</h6>
                          <p class="text-muted mb-0">Archived subcategories will appear here.</p>
                        </td>
                      </tr>
                    <% } %>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>

        <%- include('../../partials/footer') %>
        <div class="content-backdrop fade"></div>
      </div>
    </div>
  </div>

  <div class="layout-overlay layout-menu-toggle"></div>
</div>

<%- include('../../partials/scripts') %>
